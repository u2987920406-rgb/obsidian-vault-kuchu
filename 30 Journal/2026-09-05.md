# 2026-09-05 — Kitsune to Yōkai : v1 déclarée

## La passe v1 (après-midi/soir)

- **Flow complet validé par Raf** : menu → hordes → mini-boss à 60 s → victoire.
- **Trou majeur trouvé par la passe** : le mini-boss était *impossible à
  rencontrer* — depuis le spawn continu, `waves_finished` n'était plus jamais
  émis. Corrigé : boss à `BOSS_TIME=60s` (signal `boss_time`), test E2E boss 5/5.
- **Équilibrage** (retours Raf) : difficulté up (hordes 2-3, intervalle
  1.2→0.55 s), boss up (70 PV, rapide, rafales 7), sprites plus gros puis
  kitsune recalé 1.4, visée droite fluide (slerp 0.65, plus de snap).

## Le long débogage visuel (le fil rouge de la journée)

- **Voile gris** : deux `Panel` (JoystickArea/FireZone) plein écran — leur
  `color=(0,0,0,0)` est ignoré sur un Panel (style box sombre par défaut).
  → `ColorRect` transparents. Trouvé grâce au **damier de debug** demandé par Raf.
- **Décor non bouclable** : vignettage + gradient vertical de la texture IA
  (haut 16 vs bas 90 de luminosité) → neutralisé par correction colorimétrique.
- **Kitsune invisible** : tuiles ajoutées après Player → z-order cassé.
  → test E2E « z-order » ajouté (régression impossible).
- **Sprites damier** : transparence IA aplatie en pixels → flood fill alpha.
- **Game over décentré** : `get_tree().root.size` (physique) vs viewport logique.
- **Cache web** : Raf rejouait des versions fantômes → cache-bust `?v=timestamp`.

## Le process qui a fini par marcher

- **Capture Xvfb + analyse vision** avant chaque livraison visuelle (mes yeux
  sur le jeu, plus seulement la logique).
- **Délégation à des subagents forts** (provider `nous`) pour les bugs bloquants
  et les features groupées — le tactile a été résolu en 76 s par un subagent
  après une journée d'entêtement de ma part.

## État

- v1 déclarée. Tests : 10/10 gameplay + 5/5 boss + 15/15 game over.
- Backlog v2 formalisé dans [[10.04 Kitsune to Yōkai/Bilan-v1]].
- Tunnel trycloudflare **temporaire** (URL change) → tunnel nommé à prévoir.
