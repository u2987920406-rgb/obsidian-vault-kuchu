# 2026-09-15

## Gel noyau et reprise à distance (matin)

Le BMAX a été **totalement injoignable de ~11:59 à 16:53**, jusqu'à un hard
reboot manuel de Raf.

### Diagnostic (mesuré, pas deviné)

Dernière ligne écrite dans le journal : **11:59:02**. Aucune séquence d'arrêt
(pas de SIGTERM, pas de « Journal stopped ») — contrairement aux arrêts propres
du 30/08. Aucun vmcore dans `/var/crash` alors que kdump est actif : le noyau
n'a même pas eu le temps de paniquer.

**Ce n'était pas Tailscale.** Tout le noyau a gelé ; tailscaled est mort avec
la machine. Une fois le noyau figé, plus rien ne tourne : ni SSH, ni systemd,
ni Hermes, ni Docker.

**Suspect principal : le pilote `amdgpu`** (Radeon 780M du Ryzen 8745HS).
Signes avant-coureurs dans le journal : `dm_irq_work_func [amdgpu] hogged CPU
for >10000us` (répété) et `REG_WAIT timeout ... optc314_disable_crtc line:145`.
Suspect étayé, pas prouvé — sans dump.

**Écarté** : RAM (60 % libre à 11:50), disque (17 %), veille (masquée), OOM
(les kills datent du 07/09 et visaient Chrome).

### La cause racine du « aucun moyen d'entrer »

Deux lacunes, pas une :

1. **Aucune protection automatique côté machine.** `RuntimeWatchdogUSec=0`,
   zéro `/dev/watchdog`, `kernel.panic=0` — aucun watchdog, aucun reboot
   automatique. Le chipset AMD FCH sait pourtant le faire (`sp5100_tco`).
2. **La surveillance tournait sur la machine surveillée.** Uptime Kuma est un
   conteneur du BMAX : machine gelée = Kuma gelé = aucune alerte. Le
   `hermes-healthcheck.timer` dont Raf se souvenait est un *détecteur*, pas un
   *acteur* — et il a le même angle mort.

### Livré

- `~/docker/stack/setup-resilience.sh` — watchdog matériel (`sp5100_tco`, 60 s)
  + `kernel.panic=10` + Wake-on-LAN (était `disabled`). Idempotent, une seule
  commande sudo.
- `~/docker/stack/external-monitor-ping.sh` + `setup-external-monitor.sh` —
  sonde de surveillance externe (healthchecks.io / Better Stack), signal chaque
  minute, **réarmement automatique en `@reboot`** (indispensable : sinon la
  machine redémarre par watchdog mais reste muette pour la surveillance).
- Fiche opérationnelle : nouvelle **section 17** (diagnostic, les 3 étages,
  l'ordre des gestes en voyage, recours SysRq).
- Journal : ce fichier.

### Tests réels

- Syntaxe des 3 scripts : OK.
- Garde-fou root : refuse de tourner sans privilèges (exit 1).
- Sonde non configurée : sort en 0 sans bruit (pas de spam cron).
- **Ping de bout en bout** contre un serveur HTTP local qui journalise : les 3
  modes (`test`/`up`/`start`) arrivent avec les bons paramètres URL-encodés.
- **Chaîne cron complète** : lignes installées, déclenchement réel observé à
  17:18:01 (`CRON[10602]: (raf) CMD ...ping.sh up`), requête reçue, fichier
  d'état écrit. Test nettoyé ensuite, zéro résidu.

### Test A — preuve que le watchdog REDÉMARRE vraiment (RÉUSSI)

Testé à la demande de Raf, **avant** de committer : je ne voulais pas figer une
procédure non prouvée.

`test-watchdog.sh` journalise l'état AVANT (dont le `boot_id`), charge le
module, puis ouvre `/dev/watchdog` sans jamais le nourrir.

**Résultat mesuré :**

- module `sp5100_tco` → `SP5100 TCO timer [version 0]`, timeout 60 s armé
- dernier log avant reset **17:37:44** → premier log après **17:38:50** :
  **66 s de silence**
- **aucune séquence d'arrêt propre** (contrôlé : les lignes « Journal stopped /
  SIGTERM » existent bien dans le boot d'avant, donc le contrôle est valide) —
  coupure brutale, signature d'un reset matériel
- `boot_id` différent, uptime reparti de zéro

La machine a redémarré **seule**, sans intervention. **Étage 1 : réel, pas
théorique** sur ce matériel.

Piège évité : mon premier garde-fou annonçait « 2 lignes d'arrêt » — j'ai
vérifié avant de conclure, elles appartenaient au hard reboot de 16:53. Un
compteur brut n'est pas un verdict.

### Étage 1 rendu permanent (vérifié)

Le test ne faisait que charger le module À CHAUD. `setup-resilience.sh` (lancé
par Raf — sudo exige une auth interactive que l'agent ne peut pas fournir) a
rendu le tout persistant. Vérifié après coup :

- les 4 fichiers existent : `modules-load.d/watchdog-bmax.conf`,
  `systemd/system.conf.d/watchdog-bmax.conf`, `sysctl.d/99-resilience-bmax.conf`,
  `udev/rules.d/90-wol-bmax.rules`
- `kernel.panic = 10`, `kernel.panic_on_oops = 1`
- Wake-on-LAN : `disabled` → **`enabled`**
- `RuntimeWatchdogUSec=1min` (l'ancien était `0`), systemd dit
  `Using hardware watchdog /dev/watchdog0: 'SP5100 TCO timer'`
- `wdctl` : `Timeleft: 60 seconds`, état `active`, `KEEPALIVEPING=1`
- `systemd-modules-load.service` est `static`/`active` → le module se
  rechargera seul au démarrage

### Leçon

Pour savoir qu'une machine est morte, la surveillance doit être **hors de
cette machine**. Un détecteur local ne sert à rien contre un gel noyau : il
faut un acteur automatique (watchdog matériel), sinon l'alerte et
l'intervention viennent forcément de l'extérieur.

Un watchdog reste inutile si rien ne le **réarme** après le redémarrage : le
`@reboot` de la sonde fait partie de la chaîne, pas un détail.

**Et surtout : prouver, pas supposer.** Charger un module ne prouve pas qu'il
redémarre la machine. Le test par gel volontaire était la seule façon de
savoir — et il valait la peine d'être fait avant de graver la procédure.

### Limite honnête

Le watchdog couvre le gel **logiciel** (noyau figé, pilote en boucle). Il ne
couvre pas le gel **matériel** (alim, RAM, CPU) : là, le timer TCO ne tourne
plus non plus. D'où les étages 2 et 3.

### Étage 2 — healthchecks.io (PROUVÉ, sans sudo)

Pour savoir qu'une machine est morte, la surveillance doit vivre **hors** de
la machine. Uptime Kuma est disqualifié : il tourne sur le BMAX, donc il gèle
avec elle et ne peut pas signaler sa propre mort.

**Choix : healthchecks.io** (gratuit : 20 sondes, alertes illimitées par mail
/ Discord / Slack / Telegram / ntfy). C'est le BMAX qui envoie un signal de
vie vers Internet ; si les signaux cessent, l'alerte part de l'extérieur.

Réglages de la sonde : **Period 5 min**, **Grace Time 15 min** (délai
d'alerte = Period + Grace ≈ 20 min), et une **intégration active** — sans
elle la panne est détectée mais personne n'est prévenu.

    bash ~/docker/stack/setup-external-monitor.sh 'URL_DE_PING'

**Aucun sudo** : tout vit dans `~/.hermes/` et dans le crontab de raf.

**Preuve mesurée :**

- 19:00:01 — le cron envoie `up` **seul** (vu dans le journal)
- 19:01:33 — signal d'échec volontaire → **alerte mail reçue**
- 19:05:01 — le cron renvoie `up` → **retour au vert automatique**
- l'IPv6 vue par healthchecks = celle du BMAX dans les logs tailscaled, donc
  c'est bien cette machine qui est surveillée

Détail de conception : cadence **5 min** alignée sur la Period. Pinger plus
souvent ne détecte rien de plus vite (le délai dépend de la Grace), et le
gratuit ne garde que 100 entrées d'historique par sonde — à 1 min cela ne
couvrirait que ~1h40.

### Reste à faire

- **Étage 2 — FAIT et PROUVÉ** (2026-09-15) : healthchecks.io branché.
- **Étage 3** : acheter une prise connectée (Tasmota/Shelly).
- **BIOS, une fois à la main** : `ErP Ready` → Disabled,
  `Power On By PCIE/PCI` → Enabled (sinon le WoL ne peut pas marcher éteint).
- Nettoyer le marqueur de test : `rm -f ~/.hermes/watchdog-test.marker`.
- Committer le vault (en attente de l'accord de Raf).
