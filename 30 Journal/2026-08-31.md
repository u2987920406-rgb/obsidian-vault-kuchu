# 2026-08-31 — Ulysse lancé sur le BMAX (handoff Claude)

## Contexte
Raf a demandé de lancer Ulysse (repo GitHub `LyssU_googlelike`) sur le BMAX.
Session interrompue : Raf passe la main à Claude. Tout est opérationnel.

## Ce qui tourne (vérifié en réel)
- **serve.py (Ulysse)** sur `127.0.0.1:8090` — page + relais authentifié.
  Clone dans `~/projets/ulysse` (branche master, repo `LyssU_googlelike`).
- **dashboard Hermes** sur `127.0.0.1:9119` (service systemd, réutilisé).
- **proxy chat** sur `127.0.0.1:8645` (provider nous).
- **Exposé tailnet** : `https://raf-bmax.tail14baaa.ts.net:8090/` (tailnet only).

## Adaptations faites au repo (différences avec le setup Windows)
1. `serve.py` : `PORT = 8080` → **8090** (8080 pris par Dozzle).
2. `ulysse-config.js` : `HERMES_URL` 9123 → **9119** (dashboard réel).
3. `serve.py` : ajout du hostname tailnet à `ALLOWED_HOSTS`/`ALLOWED_ORIGINS`
   (anti DNS-rebinding) pour l'accès téléphone.
4. **Jeton de session fixé** : `HERMES_DASHBOARD_SESSION_TOKEN=ulysse_cb09258538616565442a3bf1bfc186c3`
   ajouté au service `hermes-dashboard.service` ET passé à serve.py en env.
   → le WS `/api/ws` (chat) se connecte. Sans ça : 403 "agent non connecté".

## Vérifié
- Page `ulysse.html` : HTTP 200.
- Relais `/api/health` → dashboard : `{"ok":true,"version":"0.20.6","auth_required":false}`.
- WS `/api/ws` via serve.py : **connecté**, reçoit `gateway.ready`.
- Certificat TLS tailnet valide (Let's Encrypt), aucune alerte.

## Notes / pièges
- Le gateway webhook (8644) ne tourne pas → panneaux Webhooks inactifs.
- Le jeton est maintenant FIXE (dans le service) : un redémarrage du dashboard
  ne le change pas, Ulysse continue de marcher.
- L'écran du BMAX était verrouillé pendant la session (pas de capture possible).
- Les modifs de `serve.py`/`ulysse-config.js` sont LOCALES (non commitées) —
  décider si on les remonte sur le repo GitHub.
