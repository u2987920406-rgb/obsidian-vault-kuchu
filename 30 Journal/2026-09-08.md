# 2026-09-08 — HTML-as-Plan dans la Méthode, validé Ulysse

## Contexte
Suite à la vidéo « HTML is the new Markdown » (How I AI, Claire Vo + Tharik) :
le `.html` force la lecture réelle d'un plan là où un `.md` de 1000 lignes est
survolé. Appliqué à La Méthode + testé sur un projet réel (Ulysse).

## Ce qui a été fait
- **Sandbox HTML-as-Plan** : `~/projets/sandbox-html-plan/` (template `plan.html`
  + instance + app test `compute-cost-estimator`).
- **App test from scratch en TDD** (Node `node:test`, zéro dépendance) : 9/9 verts
  après fix d'un bug trouvé par le **QA agent** (entrée CLI non-numérique → `$NaN`
  silencieux, exit 0 ; fix minimal + test → 9/9). Clapet Hermès passé.

## Leçons / décisions
- **`plan.html` intégré à La Méthode** (skill `la-methode`) : (1) template dans
  `templates/plan.html` du skill, (2) référencé au **5e livrable** + **Étape 6bis**
  (générer le plan en HTML pour validation visuelle de Raf) + checklist
  « Verification ». `.md` = source d'écriture, `.html` = couche de lecture.
  Numérotation Étape 6bis corrigée (6/7/8).
- **Note écosystème** : `~/vault/11 Ressources/Écosystème Raf — Vue d'ensemble.md`
  → carte des 6 piliers (La Méthode, Cabinet, QA loop, Boucle rétro, HTML-as-Plan,
  Rust) + flux idée→v1. Liée dans `00 Index.md` (11 Ressources).
- **Vérif d'un livrable HTML** : pas de « c'est bon » au doigt. Valider sur jsdom
  **avec URL fournie** (`new JSDOM(html,{url:"http://localhost/…"})`) pour que
  `localStorage` existe (origin opaque → « localStorage is not defined » = harnais,
  pas le code). D'ailleurs jsdom n'est pas dans la sandbox mais dans
  `ulysse/web/node_modules` : réutilisable via `NODE_PATH`.

## Vérif rendu réel (plan-ulysse.html, pas le texte de réponse)
- KPI : 3/5 jalons · 3/6 AC · risque restant `Hi`
- Onglets (5) câblés : clic AC → pane AC active, PRD caché
- Coche AC → KPI actualisé, localStorage persisté
- **Aucune erreur JS**

## Livrables
- `~/projets/ulysse/plan-ulysse.html` commité `59049dc` (branche `v2/graph-agentique`)
- Vault : note écosystème + skill la-methode (template + étape 6bis)

## Pour la suite
- Étendre HTML-as-Plan à un autre projet (gestion-budget / Astroprisma UI) ?
- Ajouter jsdom à la sandbox pour une suite de validation rendu dédiée.
