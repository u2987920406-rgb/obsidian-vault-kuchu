# 2026-09-22

## Hermes : migration complète sur Xiaomi

- Tout basculé sur l'abonnement Token Plan Xiaomi MiMo : modèle global
  (`mimo-v2.6-flash`), fallback et vision (`mimo-v2.6-pro`), profil Ulysse,
  cerveau QA Astroprisma (correction du 401 : `OLLAMA_API_KEY` →
  `XIAOMI_API_KEY`).
- TTS et STT passés en providers command Xiaomi (`mimo-v2.5-tts` /
  `mimo-v2.5-asr`) via `~/.hermes/scripts/xiaomi_tts.py` et `xiaomi_stt.py`,
  Xiaomi n'exposant pas d'API `/v1/audio/*`.
- Exceptions : `browser.cloud_provider` et `image_gen` restent sur `nous`.
- Limite : ASR Xiaomi en `auto`/`zh`/`en` seulement → transcription française
  approximative.
- Détails et backups : [[raf-bmax — Fiche opérationnelle]] §10bis.
